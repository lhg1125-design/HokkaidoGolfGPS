# 골프애니스킬

## 목적
HokkaidoGolfGPS의 코스/Yardage 이미지를 실제 홀 형상은 그대로 유지하면서 동화·애니메이션 스타일로 렌더링하기 위한 고정 디자인 스킬이다.

## 원본 Source of Truth — 최우선 규칙
1. 야디지 참조 이미지는 반드시 Storybook/Illustrated/Pixel Master 적용 전 백업 브랜치 `backup/yardage-before-storybook-20260814`의 수집 정의를 기준으로 다시 확보한다.
2. 현재 `main` 브랜치에 남아 있는 `yardage_*` 가공본/재렌더본/깨진 결과물은 형상 참조용으로 사용하지 않는다.
3. 백업 브랜치에는 CI에서 받아오던 이미지 바이트 자체가 항상 커밋되어 있지 않으므로, 다음 백업 파일이 가리키는 공식/원본 URL에서 다시 받아 원본 세트를 재구성한다.
   - 사호로: `.github/scripts/fetch-v18-premium-course-art.sh`의 Rakuten GORA `new_hole_info` 원본
   - 내포: `.github/data/naepo-real-yardage-v1132.tsv` + `.github/scripts/fetch-naepo-real-yardage-v1132.py`의 원본/정확 crop 규칙
   - 기타 코스도 동일하게 백업 브랜치의 fetch script가 지정한 원본을 우선한다.
4. 스타일 변환을 시작하기 전에 `원본 야디지 | 생성본` 좌우 비교 이미지를 만든다.
5. 원본이 깨졌거나 누락된 홀은 생성 금지. 먼저 백업 정의에서 원본을 다시 수집한다.

## 절대 규칙: 코스 형상 잠금
1. 실제 홀 원본 야디지/코스 이미지를 좌표 기준(Source of Truth)으로 사용한다.
2. 다음 요소의 위치·크기·외곽선·상대 배치를 임의로 바꾸지 않는다.
   - 홀 전체 외곽 실루엣
   - 티잉 구역
   - 페어웨이 전체 형상 및 분기
   - 그린 위치/형상
   - 벙커 위치/형상
   - 워터/해저드 위치/형상
   - 카트도로
   - OB/HZ 경계 및 표식 위치
   - 핀 위치
3. 생성형 이미지 모델이 위 형상을 새로 상상하거나 단순화하거나 재배치하면 실패로 판정한다.
4. 원본 형상을 확보하지 못한 홀은 새로 추정하지 말고 원본을 먼저 확보한다.

## 허용되는 변경
코스 형상은 그대로 두고 아래 시각 요소만 업그레이드한다.
- 잔디: 동화책 같은 섬세한 페인터리/셀 셰이딩 질감
- 페어웨이: 원래 외곽을 유지한 채 부드러운 명암, 잔디결, 스트라이프 및 입체감 추가
- 러프: 페어웨이보다 짙고 풍부한 질감
- 워터: 위치/외곽은 고정, 내부에 파랑 그라데이션·물결·하이라이트만 추가
- 벙커: 모양 고정, 내부 모래결·밝은 하이라이트·부드러운 그림자만 추가
- 주변 수목: 기준 이미지처럼 크기와 수관 모양이 서로 다른 큰 나무를 랜덤하게 조밀하게 배치. 작은 원 반복 패턴 금지
- 꽃/수풀/바위: 코스 핵심 형상을 가리지 않는 범위에서만 소량 배치
- 전체 톤: 밝고 귀여운 동화/애니메이션, 선명한 녹색, 부드러운 음영과 3D 깊이감

## UI 디자인 마스터
- 상단 Sky Header: 밝은 파랑, 둥근 굵은 글자, 구름/날씨 요소
- 거리판: 승인된 나무판의 색상·나뭇결·음영·두께·Shadow를 그대로 유지
- FRONT / CENTER / BACK은 3열 고정
- 단위는 항상 m(미터). y/yard 사용 금지
- 하단 NAV는 진한 녹색 4개 고정: 스코어 / 코스 / 타겟 / 메뉴
- 아이콘/텍스트는 크고 한 손 조작 가능한 수준
- 레전드/거리 안내 패널은 기본적으로 표시하지 않는다

## 거리 표시 규칙
1. 고정 참고점은 화이트 티 기준 200m 지점을 코스 위에 표시한다.
2. 150m 고정 마커는 사용하지 않는다.
3. 실제 앱에서는 GPS 플레이어 현재 위치에서 실제 핀까지의 남은 거리를 계산해 말풍선으로 표시한다.
4. 남은 거리 말풍선은 이전 대형 시안의 약 80% 크기를 기준으로 한다. 글씨도 동일 비율로 축소하되 한눈에 읽혀야 한다.
5. 말풍선 텍스트 예시: `남은 거리 232m` 또는 `핀까지 232m`.
6. 말풍선/거리 UI는 코스의 핵심 벙커·워터·페어웨이 형상을 가리지 않도록 배치한다.

## Score 입력 규칙
- 타수 입력은 `-1 / 0 / 1 / 2 / 3 / 4 / 5` 상대타수 방식으로 표시한다.
- 의미: 버디 / 파 / 보기 / 더블보기 / 트리플보기 / +4 / +5.
- 실제 저장 타수 = 해당 홀 PAR + 선택값.
- `+` 단독 버튼 방식은 사용하지 않는다.
- 버튼은 승인된 Storybook 스타일의 입체감, bevel, shadow, 큰 숫자를 사용한다.

## 작업 순서
1. 백업 브랜치의 fetch 정의로 실제 홀 원본을 다시 확보
2. 원본 이미지 파일 무결성/크기 확인
3. 원본의 핵심 좌표/마스크 고정
4. `원본 | 생성본` 좌우 비교 Preview 작성
5. 코스 형상은 변경하지 않고 톤/텍스처/음영만 동화풍으로 렌더링
6. 사용자 승인 전 APK 빌드 금지
7. 승인된 홀만 다음 홀로 확장
8. 여러 홀 승인 후 일괄 APK 반영

## 검수 체크리스트
- 참조가 `backup/yardage-before-storybook-20260814` 기반 원본인가
- 현재 main의 깨진/가공된 yardage 이미지를 참조하지 않았는가
- 원본 홀과 외곽 실루엣이 동일한가
- 워터 모양/위치가 동일한가
- 페어웨이 모양/위치가 동일한가
- 벙커 모양/위치가 동일한가
- 그린/핀 위치가 동일한가
- 티/카트도로/OB/HZ가 동일한가
- 나무가 반복 원형 패턴이 아니라 자연스럽고 랜덤한 대형 수관인가
- 나무판 색·나뭇결·Shadow가 승인본과 일치하는가
- 모든 거리가 m 단위인가
- 150m 마커가 제거됐는가
- 200m 기준 마커가 있는가
- GPS→핀 남은거리 말풍선이 적절한 크기로 보이는가
- 레전드가 제거됐는가
- APK 생성 전에 Preview 승인을 받았는가

## 핵심 원칙
`BACKUP SOURCE FIRST. LAYOUT/GEOMETRY LOCK. STYLE ONLY.`
코스는 다시 그리는 것이 아니라 백업 정의에서 재수집한 실제 형상 위에 동화풍 재질을 입힌다. 사용자가 승인한 이미지 톤과 UI를 디자인 마스터로 사용하고, 임의 재해석을 금지한다.

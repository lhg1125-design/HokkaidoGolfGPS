name: Archived V1.13.6 Workflow Alias

on:
  workflow_dispatch:

jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Archived workflow path. Use build-v1136-approved-ui-hotfix.yml"

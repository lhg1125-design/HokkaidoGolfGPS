name: Build V1.18 Runtime GPS Clean APK

on:
  workflow_dispatch:
  push:
    branches: [ main ]

permissions:
  contents: read

jobs:
  runtime-build:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build Runtime APK
        run: gradle --no-daemon :app:assembleDebug

      - name: Verify APK contamination
        run: |
          APK=app/build/outputs/apk/debug/app-debug.apk
          test -s "$APK"
          if unzip -l "$APK" | grep -Ei 'royallinks|concept|golden|preview'; then
            echo 'Runtime contamination detected'
            exit 1
          fi
          echo 'Runtime APK clean PASS'

      - name: Rename APK
        run: cp app/build/outputs/apk/debug/app-debug.apk HokkaidoGolfGPS-V1.18-runtime-clean-debug.apk

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: HokkaidoGolfGPS-V1.18-runtime-clean-debug
          path: HokkaidoGolfGPS-V1.18-runtime-clean-debug.apk
          retention-days: 14
